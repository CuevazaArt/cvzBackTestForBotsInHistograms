# Flutter / Dart build artifacts (app/.gitignore covers the rest)
app/.dart_tool/
app/.flutter-plugins
app/.flutter-plugins-dependencies
app/build/
app/windows/flutter/ephemeral/
app/.idea/

# Note: generated *.g.dart / *.freezed.dart are committed (built once per change)

# Candle backup CSVs — tracked intentionally (do NOT ignore)
!data/candles/

# Env / secrets
.env
*.key
*.pem

# IDE
.vscode/
.idea/
*.swp
*.swo
*~
.cursor/

# OS
.DS_Store
Thumbs.db
desktop.ini

# Scratch / temp
temp_repos/
*.scratch.dart
